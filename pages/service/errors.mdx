import { useData } from 'nextra/data'
import { errors } from '@onbeam/errors'


export const ErrorMapping = () => {
  // Get the data from SSG, and render it as a component.
  const { mapping } = useData()
  return Object.entries(mapping).map(([key, value]) => <li>[<a href={`/service/errors/${key}`}>{key}</a>] - {value}</li>)
}
 
# Error codes

### Learn more about error codes and how to resolve them.

Some errors will return with an error code - a short string with a brief explanation. Below you'll find reference information about each expected error.

<br />

<ul>
  <ErrorMapping/>
</ul>

export const getStaticProps = () => {
  return {
      props: {
        // We add an `ssg` field to the page props,
        // which will be provided to the Nextra `useData` hook.
        ssg: {
          mapping: errors
        }
      },
      // The page will be considered as stale and regenerated every 60 seconds.
      revalidate: 60
  }
}