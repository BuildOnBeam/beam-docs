# API Reference

The Beam API is a RESTful API. Given you have obtained your api key, you are able to connect to the API by using one of our provided server-side SDKs or by communicating through the RESTful endpoints directly over HTTP.

## Specification

If you ever feel the need to access all of the available endpoints directly from our deployed service, you can use the following links

### Automation API

```md filename="Swagger UI"
https://api.beta.onbeam.com/api/automation
```

```md filename="OpenAPI specification (JSON)"
https://api.beta.onbeam.com/api/automation-json
```

```md filename="OpenAPI specification (YAML)"
https://api.beta.onbeam.com/api/automation-yaml
```


### Player API

```md filename="Swagger UI"
https://api.beta.onbeam.com/api/player
```

```md filename="OpenAPI specification (JSON)"
https://api.beta.onbeam.com/api/player-json
```

```md filename="OpenAPI specification (YAML)"
https://api.beta.onbeam.com/api/player-yaml
```
